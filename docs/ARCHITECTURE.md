# Архитектура Gheezy Crypto

## 🏗️ Обзор архитектуры

Gheezy Crypto — модульная платформа для крипто-трейдинга с четырьмя основными компонентами, работающими асинхронно.

```
┌─────────────────────────────────────────────────────────────┐
│                     TELEGRAM BOT (aiogram 3.x)              │
│                   Интерфейс пользователя                    │
└─────────────────────────┬───────────────────────────────────┘
                          │
┌─────────────────────────▼───────────────────────────────────┐
│                     FASTAPI SERVER                          │
│                   REST API + Webhooks                       │
└─────────────────────────┬───────────────────────────────────┘
                          │
    ┌─────────────────────┼─────────────────────┐
    │                     │                     │
    ▼                     ▼                     ▼
┌─────────┐         ┌─────────┐           ┌─────────┐
│ Signals │         │  DeFi   │           │  Whale  │
│ Engine  │         │Aggregator│          │ Tracker │
└────┬────┘         └────┬────┘           └────┬────┘
     │                   │                     │
     │              ┌────┴────┐                │
     │              │  Copy   │                │
     │              │ Trading │                │
     │              └────┬────┘                │
     │                   │                     │
     └───────────────────┼─────────────────────┘
                         │
    ┌────────────────────▼────────────────────┐
    │              PostgreSQL                  │
    │         (SQLAlchemy + asyncpg)          │
    └─────────────────────────────────────────┘
                         │
    ┌────────────────────▼────────────────────┐
    │                 Redis                    │
    │           (Кэширование данных)          │
    └─────────────────────────────────────────┘
```

## 📁 Структура проекта

```
gheezy-crypto/
├── docker-compose.yml    # Оркестрация контейнеров
├── Dockerfile           # Сборка образа
├── requirements.txt     # Python зависимости
├── .env.example        # Пример переменных окружения
│
├── src/
│   ├── __init__.py
│   ├── main.py         # Точка входа
│   ├── bot.py          # Telegram бот (aiogram)
│   ├── config.py       # Pydantic Settings
│   │
│   ├── signals/        # 🤖 AI Сигналы
│   │   ├── analyzer.py     # Генерация сигналов
│   │   └── indicators.py   # RSI, MACD, Bollinger
│   │
│   ├── defi/           # 🏦 DeFi Агрегатор
│   │   └── aggregator.py   # APY ставки
│   │
│   ├── whale/          # 🐋 Whale Tracker
│   │   └── tracker.py      # Отслеживание китов
│   │
│   ├── copy_trading/   # 📊 Copy-Trading
│   │   └── system.py       # Система копирования
│   │
│   └── database/       # 💾 База данных
│       ├── db.py           # Подключение
│       └── models.py       # SQLAlchemy модели
│
└── docs/
    ├── ARCHITECTURE.md  # Этот документ
    └── DEPLOYMENT.md    # Инструкция по деплою
```

## 🔧 Компоненты системы

### 1. AI Signals Engine (src/signals/)

Генерация торговых сигналов на основе технического анализа:

- **RSI (Relative Strength Index)** — определение перекупленности/перепроданности
- **MACD** — определение тренда и импульса
- **Bollinger Bands** — анализ волатильности

```python
# Пример использования
from src.signals import SignalAnalyzer

analyzer = SignalAnalyzer()
signal = await analyzer.analyze("bitcoin")
print(signal.explanation)  # Человекочитаемое объяснение
```

### 2. DeFi Aggregator (src/defi/)

Агрегация данных о DeFi протоколах через DefiLlama API:

- Получение топ протоколов по TVL
- Поиск лучших APY ставок
- Фильтрация по стейблкоинам

### 3. Whale Tracker (src/whale/)

Мониторинг крупных транзакций:

- Отслеживание переводов > $100K
- Определение направления (биржа/кошелёк)
- Анализ рыночного сентимента

### 4. Copy-Trading System (src/copy_trading/)

Система копирования сделок:

- Рейтинг трейдеров по производительности
- Подписка на сигналы трейдеров
- Автоматическое копирование сделок

## 📊 Модели данных

### User
```python
User:
    - telegram_id: int
    - username: str
    - is_premium: bool
    - portfolios: List[Portfolio]
    - trades: List[Trade]
    - alerts: List[Alert]
```

### Signal
```python
Signal:
    - symbol: str
    - signal_type: buy/sell/hold
    - confidence: Decimal
    - rsi_value: Decimal
    - macd_value: Decimal
    - explanation: str
```

### WhaleTx
```python
WhaleTx:
    - tx_hash: str
    - blockchain: str
    - amount_usd: Decimal
    - is_exchange_deposit: bool
```

## 🔄 Потоки данных

### Запрос сигнала

```
1. Пользователь: /signal BTC
2. Bot → SignalAnalyzer
3. SignalAnalyzer → CoinGecko API (история цен)
4. SignalAnalyzer → Расчёт индикаторов
5. SignalAnalyzer → Генерация объяснения
6. Bot ← Форматированный ответ
7. Пользователь ← Сообщение с сигналом
```

### Отслеживание китов

```
1. WhaleTracker → Etherscan API
2. WhaleTracker → Фильтрация по сумме
3. WhaleTracker → Определение адресов бирж
4. WhaleTracker → Анализ сентимента
5. Пользователь ← Уведомление
```

## 🔐 Безопасность

- Все токены хранятся в переменных окружения
- Подключение к БД через SSL в production
- Rate limiting для внешних API
- Валидация входных данных через Pydantic

## 📈 Масштабирование

- Горизонтальное масштабирование через Docker Swarm/Kubernetes
- Redis для кэширования и очередей
- Асинхронная обработка всех I/O операций
- Connection pooling для PostgreSQL
